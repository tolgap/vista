<h1>Listing websites</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Version</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @websites.each do |website| %>
  <tr>
    <td><%= website.name %></td>
    <td><%= website.version %></td>
    <td><%= link_to 'Show', website %></td>
    <td><%= link_to 'Edit', [:edit, @server, website] %></td>
    <td><%= link_to 'Destroy', website, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Website', new_server_website_path(@server) %>
